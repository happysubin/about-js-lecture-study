extends layout

block content
    form(method="POST")
        input(placeholder="Title", required, name="title" type="text")
        input(placeholder="Descriptions", required, name="description" type="text")
        input(placeholder="Hashtags, separated by comma", required, name="hashtags" type="text")
        input(type="submit", value="Upload Video")