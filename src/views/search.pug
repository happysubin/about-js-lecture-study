extends layout/layout.pug
include mixins/video

block content 
    h1 search
    form(method="get")
        input(type="text" name="searchTerm" )
        input(type="submit")
    if videos
        each video in videos 
            +video(video)

